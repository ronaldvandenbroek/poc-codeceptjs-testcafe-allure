{
  "name": "poc-codeceptjs-testcafe-allure",
  "version": "0.0.1",
  "description": "A proof of concept testing the coupling between CodeceptJS, TestCafé and Allure.",
  "main": "",
  "scripts": {
    "test": "npx codeceptjs run --steps --plugins allure",
    "test:serve": "npx codeceptjs run --steps --plugins allure && allure serve ./output"
  },
  "keywords": [],
  "author": "Ronald van den Broek",
  "license": "MIT",
  "devDependencies": {
    "codeceptjs": "^2.5.0",
    "testcafe": "^1.8.2"
  }
}
